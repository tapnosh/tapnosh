import { createLogger } from "./index";

/**
 * Sitemap logger for SEO and sitemap generation
 */
export const sitemapLogger = createLogger("sitemap");

/**
 * Robots.txt logger
 */
export const robotsLogger = createLogger("robots");
